# agent-todo (split workspace)

This workspace contains three subprojects:

- `backbone-api/` – todo/reminder backend + dashboard
- `telegram-relay/` – Telegram relay + PI auto prompts + reminder webhook receiver
- `pi-extension-backbone-client/` – pi extension that calls the backbone API

See each subproject README for setup.
